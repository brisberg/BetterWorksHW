general:
  branches:
    ignore:
      - gh-pages

machine:
  environment:
    GIT_DEPLOY_USERNAME: brisberg
    GIT_DEPLOY_EMAIL: brandon.risberg@gmail.com

deployment:
  production:
    branch: master
    owner: brisberg
    commands:
      - bin/circle.sh
